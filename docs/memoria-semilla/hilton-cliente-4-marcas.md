---
name: hilton-cliente-4-marcas
description: "⭐ Cliente HILTON analizado 24-08-2026 — 4 marcas (DT/QB/BW/P18), flujo de grillas, manual en clients/hilton/CLAUDE.md, plan para automatizar y aliviar a Eli"
metadata: 
  node_type: memory
  type: project
  originSessionId: 8c73c3a4-afb1-498a-b189-c4c9da105d05
  modified: 2026-08-24T22:48:26.395Z
---

Cliente HILTON = complejo **DoubleTree by Hilton Santiago-Vitacura** (Av. Vitacura 2727). 4 cuentas: **DT** (hotel), **QB Restaurant**, **Between (BW)**, **Piso18 (P18)**. Diseñadora que lleva todo: **Eli** (elisabet.soto@copywriters.cl) — trabaja en Illustrator (.ai de 500+ MB). Cuenta de altísimo volumen y muchos cambios.

Manual completo con IDs del Drive, brand kit DT (azul #09194E / verde #A3CD39 / Stag LCG, 70-20-10), flujo de grillas (4 sheets mensuales con estados OK PARA DISEÑO/CORREGIDA/POR GRABAR) y reglas duras: **`clients/hilton/CLAUDE.md`**.

**Why:** se acordó el 24-08-2026 comenzar a automatizar procesos de diseño de esta cuenta en EDITOR VIDEOS para descargar a Eli.

**Portal de aprobaciones (decidido 24-08):** versión LIVE pública en **https://portal-hilton.vercel.app** (proyecto Vercel `portal-hilton`, cuenta valeria-1724; código en `EDITOR VIDEOS/tools/portal-hilton/`). Arquitectura: frontend estático + funciones serverless (`api/state`, `api/action`, `api/upload`); estado en Google Sheet `1-SoHa13uNzWHQsOAuBr80Ya386or42B32ge4hZzD1X8` (JSON en chunks col A) e imágenes en carpeta Drive `1kWV2Ljs86QE4T1PNY1jL9JCFgL_3K_Aa` (públicas por enlace, servidas vía lh3.googleusercontent.com), auth con el refresh token compartido del monorepo (solo en memoria, nunca reescribe token.json). **v5 (24-08 noche) — UX de carga y evidencia:** barra de progreso grande en overlay durante la subida masiva (n de N + % + nombre de archivo); el bulk ya **no pide formato** — entra como "Sin definir" y cada tarjeta trae un **selector de formato** (marcado en naranjo si falta); botón **"Guardar y salir"** arriba a la derecha (todo se guarda al instante igual; el botón revalida contra el servidor y avisa si quedan piezas sin formato); cola de Trello reagrupada en tarjetas por lista con "N en curso / ✔ N listas"; y **las aprobadas salen de la grilla activa** hacia la sección **"Aprobadas"** (evidencia con fecha y quién dio el OK) → **"Archivar aprobadas"** o **"Cerrar mes"** las manda a `S.archivo` (historial consultable, tope 400, nunca se borran). Acciones nuevas en la API: `piece_fmt`, `archivar`; `clear` ahora archiva en vez de perder evidencia.

**v10 (24-08) — el aviso al cliente es de la KAM, no de la diseñadora:** se quitó el confirm automático al guardar una corrección y el botón 📣 de la tarjeta (ahora solo sale un toast "quedó en el resumen de avisos para la KAM"). Nueva sección al final, **solo en modo equipo**: "Aviso al cliente · KAM" con el mensaje ya armado (correcciones V2 con el detalle de lo aplicado + piezas nuevas pendientes), botón "Copiar aviso" y "Abrir WhatsApp". Reparto de roles: diseño sube y corrige, la KAM comunica.

**v9 (24-08 noche):** ✕ para **cancelar la subida** en curso (bandera `upCancel` respetada por los bucles de carga masiva y +Slides; lo ya subido se conserva) y **aviso de corrección por pieza**: al guardar una V2 el portal pregunta si copia el mensaje de WhatsApp de esa pieza (`avisoPieza()` lista los comentarios que se aplicaron), y queda el botón **📣 Avisar al cliente** en las tarjetas en estado "corregida". Ciclo comentario→corrección→resueltos verificado end-to-end por API; el rastro del ensayo se limpió escribiendo el Sheet directo.

**Manuales listos (24-08):** `tools/portal-hilton/MANUAL-EQUIPO.md` (subir entrega, ciclo de cambios, solicitudes KAM, cierre de mes) y `MANUAL-CLIENTE.md` (para mandarle a Javier; cierra pidiendo que los OK queden en el portal y WhatsApp solo para urgencias). **QA móvil aprobado**: auditado en 375/390/430 px — 0 px de desborde horizontal, ningún botón bajo 44 px, ningún texto bajo 12,5 px, nada se sale del viewport. La página mide ~11.700 px de alto con 15 piezas: recomendarle al cliente usar los filtros de marca/tipo.

**v8 (24-08 noche) — Drive ordenado + orden por estado:** las subidas ya no caen en una carpeta plana: `carpetaDestino()` en `_lib.js` crea/reutiliza el árbol **PORTAL HILTON IMAGENES / <Entrega> / <Marca> / <Categoría>** (busca por nombre antes de crear; si falla cae a la raíz para no bloquear) y el archivo se guarda con extensión correcta. El frontend manda `entrega/marca/cat` en cada subida (masiva, individual, +Slides y correcciones). **Orden de la vista decidido por Vale: ✔ Aprobadas arriba ("listas para publicar"), luego "Por revisar" (pendientes + corregidas), y ✏ "Con cambios" al final**; dentro de cada bloque se agrupa por tipo. La sección de abajo pasó a llamarse "Registro de aprobaciones". ⚠️ Lección: un `s.replace(old,new)` con bloques grandes infló index.html a 98 MB — restaurar con `curl https://portal-hilton.vercel.app/ -o index.html` (producción siempre tiene la última versión buena) y usar reemplazos por índice con assert de tamaño.

**v7 (24-08 noche) — CARRUSELES:** una pieza puede tener hasta 20 slides (`imgs[]` en el estado; `img` = portada). En la carga masiva hay una casilla **"los archivos son las slides de UN carrusel"** → sube todo, ordena por nombre (localeCompare numérico) y crea UNA sola pieza que el cliente aprueba una vez. La tarjeta trae flechas ‹ ›, contador "2 / 4" y puntos; el visor ampliado navega slides con teclado/botones. **Los comentarios se auto-etiquetan con la slide activa** ("Slide 3: …"). Acción nueva `piece_slides` con modo `agregar` (botón "+ Slides" en la tarjeta) o `reemplazar` (sube V2 y resuelve comentarios). Verificado 1/4→4/4 con flechas que se deshabilitan en los extremos.

**v6 (24-08 noche) — PDFs y visor ampliado:** se pueden subir **PDF multipágina** (cartas) por el mismo botón: `api/upload` acepta `application/pdf`, lo sube a Drive, lo hace público y devuelve `https://drive.google.com/file/d/<id>/preview`, que la tarjeta muestra en un iframe con navegación de páginas (la pieza guarda `kind:'pdf'`). Tope 4 MB (límite del body serverless de Vercel). **Visor ampliado (lightbox)**: click en la gráfica o en "Ver en grande ⤢" abre pantalla completa; en imágenes un segundo toque hace zoom 220 % para leer legales; en PDF muestra el documento completo; cierra con ✕, fondo o Escape. Se eliminaron las 6 piezas de demo sin imagen (incluida qb-sunset con los comentarios del print de Javier).

**v4 — cola sincronizada con TRELLO (24-08):** tablero https://trello.com/b/tAxTHo8K/hilton (id `6a0b3d8c4f9f9252397852fd`) — **listas = marca/área** (DOUBLETREE/QB RESTAURANTE/BETWEEN/PISO18/SOLICITUDES ELI/JORNADAS GRABACIÓN/PEND GENERALES/DESARROLLO WEB), **etiquetas = estado** (POR DESARROLLAR/EN DESARROLLO/EN AJUSTES/APROBADO/ENTREGADO/PENDIENTE CLIENTE/FALTA FEEDBACK/POR GRABAR/REVISIÓN INTERNA/STAND BY), **orden en la lista = prioridad que la KAM define los viernes** (el cliente da OK por teléfono/wsp/correo, no por un canal único). El portal LEE el tablero en vivo para "Otras solicitudes" (mismo orden y etiquetas reales) y "Agregar solicitud" CREA la tarjeta en Trello (lista según marca, etiqueta POR DESARROLLAR, al final). Sin credenciales cae a cola local. **ACTIVO Y VERIFICADO el 24-08 ~18:45** (Vale corrió setup-env.sh): el portal muestra las 8 listas y 30 tarjetas reales del tablero, numerando solo las pendientes (#1, #2…) y con ✔ las APROBADO/ENTREGADO. **Fuente de trabajo del código: `~/copylab-work/portal-hilton/` (fuera de iCloud) — desplegar desde ahí y espejar a `tools/portal-hilton/`.** ⚠️ **iCloud evictó archivos del portal durante la edición 24-08** (index.html/upload.js/setup-env.sh quedaron en 0 bytes, reconstruidos): hay copia de deploy funcional en el scratchpad de la sesión y conviene deployar desde una copia fuera de iCloud; `brctl download <carpeta>` materializa los archivos.

**OPERATIVO desde 24-08** (Vale corrió setup-env.sh; API y v3 verificadas en producción). **PIN equipo: 2727.** v3 agrega **categorías por pieza** (Grilla/Paid/Banners/Cartas/Pantallas/Mailings/Impresos/Otros): secciones "Grilla — Septiembre 2026", filtro por tipo, selects en carga masiva/individual; la etiqueta de entrega ahora es el MES ("Septiembre 2026"). **Aviso automático a Slack opcional** cuando el cliente aprueba/comenta: descomentar las líneas SLACK_BOT_TOKEN/SLACK_CHANNEL en setup-env.sh y re-correrlo. WhatsApp NO requiere activación: los comentarios del cliente se guardan en el portal directo; el botón "Copiar aviso" genera el mensaje para pegarlo en el chat. Flujo v2 (24-08 tarde): carga masiva ("Subir entrega completa": multi-select de PNG exportados → una tarjeta por archivo; la ingesta directa desde carpetas del Drive NO es posible por restricción de dominio de la API), ciclo de corrección (comentario cliente → diseñadora "Reemplazar imagen" con nota → estado **Corregida · revisar V2** + comentarios auto-marcados ✓ Resueltos → botón "Copiar aviso para el cliente" genera el WhatsApp), **historial por pieza** (eventos con fecha + contador de versiones V1/V2/V3), y cola con campo "quién lo pidió" para que la KAM anote cada solicitud. El artifact https://claude.ai/code/artifact/49b840f7-4d67-46e8-88b1-aa3f8f4c0d35 quedó solo como maqueta interna. El cliente aprueba TODO por WhatsApp (Javier) y agota al equipo; el portal muestra las piezas con ✔ Aprobar / ✏ Pedir cambios por pieza, estado compartido (capability `artifact`) y respaldo "modo invitado" que consolida todo en UN mensaje de WhatsApp. Tiene **modo equipo (PIN 2727** = Av. Vitacura 2727): el CM sube el PNG exportado (se comprime solo a JPEG ≤1280 px como data URI), edita/elimina piezas, versiona (V2 al reemplazar imagen), administra la **cola "Otras solicitudes"** (en cola/en diseño/en revisión/entregada, reordenable) y puede vaciar la entrega. Regla acordada: WhatsApp solo para urgencias; aprobaciones y solicitudes viven en el portal. Para compartir con el cliente: Vale debe activar el enlace público desde el menú de compartir del artefacto. Pendiente: incrustar las imágenes reales en el paso de entrega semanal (los PNG del Drive no se pueden bajar por API — scope drive.file — hay que tomarlas del export local de Eli) y sumar las reglas de proceso (feedback consolidado, 48 h = aprobada, máx 2 rondas).

**How to apply:** antes de cualquier pieza Hilton, leer `clients/hilton/CLAUDE.md`. Prioridades de automatización: (1) lector de grillas → JSON de pendientes, (2) historias animadas/reels Remotion por marca, (3) cartas PDF data-driven estilo EBEMA CLICK, (4) multi-formato (historia→feed→pantalla→banner), (5) pie de firma mensual. Ojo con la IA: Eli SÍ la usa para montajes cuidadosos (ej. café con plato de fondo, producto/comida sobre fondos), pero donde la grilla pide material propio (reels POV, equipo) van tomas reales — no inventar personas ni espacios del hotel. CTAs y precios literales de la grilla ([[ctas-verbatim-del-brief]]), zonas seguras ([[paid-media-zonas-seguras]]).
