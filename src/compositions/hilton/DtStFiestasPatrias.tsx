/**
 * DOUBLETREE — HISTORIA · SALUDO FIESTAS PATRIAS (STORIES col H · 18-09 · 09:00)
 *
 * Encargo de Eli, 15-09-2026: «Trabajaremos diseñando una historia estática para
 * DT […] la del 18 de septiembre con una de estas imágenes de carpeta […] acá no
 * pasa nada si se ven los rostros, por formato de storie. Guíate del brief.»
 *
 * ⭐⭐ RONDA 2 — «TE DEJO REFERENCIA DE CÓMO DEBE QUEDAR». La ronda 1 fue UNA
 * foto a sangre con el aparato de la ST del Día del Turismo. Eli mandó una
 * referencia armada y cambia la pieza entera. Lo que la referencia decide:
 *
 *   1. **Es un COLLAGE**, no una foto sola.
 *   2. El titular va en **ITÁLICA** y parte «¡Felices Fiestas» / «Patrias!».
 *   3. La bajada va en **VERSALES**, también itálica, en tres líneas.
 *   4. El logotipo bajaba al tercio inferior — ⛔ **revertido en la ronda 3**.
 *   5. Nada de marco ni de regla: el recurso gráfico es el mosaico.
 *
 * ⭐⭐ RONDA 3 — cuatro correcciones de Eli sobre el PNG: «Deja el logo principal
 * arriba donde debe estar, ajusta un poco el espaciado de abajo de los párrafos
 * están muy separados de entre párrafos, el fondo que sea el azul DT no blanco.
 * Con esos ajustes, está quedando muy bien.»
 *
 *   · **El logotipo vuelve ARRIBA**, a la plantilla `logo-ST.png` (167 @ y 241).
 *     La plantilla de márgenes es de la marca y manda sobre la referencia.
 *   · **Los interlineados se aprietan** — ver `INTERLINEA`.
 *   · **El hilo entre cuadros pasa de blanco a AZUL DT** — en el script del
 *     mosaico. En blanco el mosaico se leía como diez piezas sueltas.
 *
 * ⭐⭐ RONDA 4 — «agranda más el logo, para compensar un poco la jerarquía y es un
 * mensaje de saludo, faltaría unos detalles del 18 de septiembre de Chile, muy
 * sutil, bandera algo que se vea de una esquina.»
 *
 *   · **El logotipo sube a 225** (era 167, el de la plantilla). Con el titular
 *     al 75 % del ancho, el logotipo al 15,5 % no sostenía el otro extremo de la
 *     jerarquía; al 20,8 % sí. ⚠️ Se aparta de la plantilla de márgenes SOLO en
 *     tamaño: sigue centrado, en su tope de 241 y a su proporción real.
 *   · **Entra la bandera** — ver `BANDERAS`.
 *
 * ⭐ RONDA 5 — «te dejé referencia de dónde y cómo debe quedar la bandera, lo demás
 * está excelente». Eli marcó **DOS** banderas rojas FLANQUEANDO el bloque de texto,
 * no una en la esquina. Se corrige eso y nada más: el logotipo, los interlineados,
 * el mosaico y los textos quedan como estaban.
 *
 * ⚠️ Se calcó de la CAPTURA que ella pegó en el chat, no de un archivo: las
 * proporciones son lectura de pantalla. Si manda el original, se re-mide.
 *
 * ══════════════════════════════════════════════════════════════════════════
 * EL BRIEF, LITERAL — no se toca ni una palabra (§G: en DT sólo se DISEÑA)
 * ══════════════════════════════════════════════════════════════════════════
 * Hoja STORIES, columna H, estado **OK PARA DISEÑO**
 * (instantánea `clients/hilton/grillas/dt-septiembre-2026.md`, 15-09-2026):
 *
 *     ESTÁTICA - SALUDO INSTITUCIONAL FIESTAS PATRIAS
 *     Por confirmar: opción de collage (queda pendiente, no resolver sin
 *     aprobación del cliente).
 *     Diseño con ambientación institucional Fiestas Patrias (colores nacionales,
 *     elementos sutiles). Mensaje de agradecimiento a huéspedes y colaboradores.
 *     Texto principal: "¡Felices Fiestas Patrias!"
 *     Subtexto: Gracias por ser parte de nuestra familia DoubleTree.
 *     Por confirmar: opción de collage con fotos del equipo/huéspedes (queda
 *     pendiente).
 *
 * Los dos textos van **verbatim**; lo único que cambia es su CAJA —la bajada se
 * compone en versales— y eso es tipografía, no redacción.
 *
 * ⭐ **El collage lo destraba ELI, no yo.** El brief lo dejaba «por confirmar» y
 * por eso la ronda 1 fue de una foto. Ella manda la referencia con el collage
 * armado: la decisión es suya y la aprobación del cliente también (§G).
 *
 * ══════════════════════════════════════════════════════════════════════════
 * ⚠️ DOS COSAS QUE CHOCAN CON REGLAS ESCRITAS, Y MANDA LA REFERENCIA
 * ══════════════════════════════════════════════════════════════════════════
 * · **La línea LARGA queda arriba.** El repertorio de DT dice corta arriba,
 *   larga abajo (así está «¡Feliz Día» / «del Turismo!»). Acá el corte lo fija
 *   el titular de la referencia: «¡Felices Fiestas» / «Patrias!».
 * · **Las versales van en Stag y no en Trade Gothic.** El manual oficial le da
 *   a Trade las versales; la referencia las pone en serif itálica. Manda la
 *   diseñadora — es la jerarquía que ella misma fijó el 08-09: «el manual es un
 *   apoyo, pero al final la diseñadora soy yo».
 *
 * Quedan anotadas para que se lean como decisión y no como descuido.
 *
 * ══════════════════════════════════════════════════════════════════════════
 * ⛔ LOS ROSTROS: LA EXCEPCIÓN LA AUTORIZÓ ELI, NO LA INVENTÉ YO
 * ══════════════════════════════════════════════════════════════════════════
 * §A de DT dice que en imagen no va el rostro de un trabajador. Eli la levantó
 * **para esta pieza y por el formato**: «acá no pasa nada si se ven los rostros,
 * por formato de storie». Es autorización puntual, **no** precedente: la próxima
 * pieza de DT vuelve a §A salvo que ella diga lo contrario.
 *
 * ══════════════════════════════════════════════════════════════════════════
 * EL MOSAICO — diez cuadros, todos del cliente, nada generado
 * ══════════════════════════════════════════════════════════════════════════
 * Se arma aparte, en `scripts/dt-st-fiestas-collage.py`, y entra como una sola
 * imagen. Ahí viven la geometría de los diez cuadros, los dos cortes en
 * diagonal, el filete claro y el velo.
 *
 * ⭐ El mosaico NO es una grilla pareja: los anchos de la columna izquierda van
 * cambiando (402 · 438 · 396 · 420) y hay dos cortes en diagonal. Una grilla
 * 2×5 perfecta se lee como plantilla, que es justo lo que la referencia no hace.
 *
 * ⭐ Y los «colores nacionales» que pide el brief los pone el MATERIAL —los
 * vestidos de cueca, la bandera del escenario, el faldón tricolor del buffet—,
 * así que no hay ni un adorno pegado encima.
 *
 * ══════════════════════════════════════════════════════════════════════════
 * LOS CUERPOS — calcados de la referencia, medidos con PIL
 * ══════════════════════════════════════════════════════════════════════════
 * En la referencia el titular ocupa ≈77 % del ancho. «¡Felices Fiestas» en Stag
 * Medium Italic da 816 px al cuerpo **128** = 76 %. Las dos líneas van al MISMO
 * cuerpo y lo que las separa es el peso —Medium arriba, Light abajo—, que es el
 * criterio que Eli fijó el 10-09.
 *
 * La bajada va al cuerpo **50** con tracking de versales (0,04em): en versales
 * sin tracking las letras se apelmazan, y eso ya costó una ronda en Between.
 *
 * ⛔ **Stag no puede escribir `¡`**: los nueve cortes traen el mismo subconjunto
 * de 354 glifos y a todos les falta `U+00A1`. Se resuelve con el truco de Eli
 * (`volteaApertura`): el signo de cierre rotado 180°.
 *
 * ══════════════════════════════════════════════════════════════════════════
 * LA REJILLA (1080×1920; se entrega a 2250×4000, ×2,0833)
 * ══════════════════════════════════════════════════════════════════════════
 *    250 ─ fin de la zona segura superior de Instagram
 *    241 ─ tope del logotipo — centrado, ancho 225 (ronda 4)
 *    675 ─ tope de la tela de las dos banderas, flanqueando el bloque de texto
 *    428 ─ pie del logotipo
 *    809 ─ punta de los mástiles, en el canto mismo de la pieza (x=0 y x=1080)
 *    540 ─ tope de la caja del titular
 *    815 ─ tope de la bajada en versales
 *   1580 ─ empieza la zona segura inferior — se deja limpia
 *
 * ⭐ El logotipo va a la plantilla de márgenes de Eli y se escala **uniforme**
 * por su proporción real (1,2254). El logotipo no se deforma nunca.
 *
 * ⚠️ La columna H no trae campo INTERACCIÓN, así que no hay sticker que
 * reservar. Igual la franja de abajo queda limpia: la interacción no se dibuja.
 */
import React from 'react';
import {AbsoluteFill, Img, staticFile} from 'remotion';

import {DT, cargarFuentesDT, volteaApertura} from '../../brand/doubletree';

cargarFuentesDT();

const G = DT.geometria;

/** Textos LITERALES del brief. No se editan (§G); sólo cambia su caja. */
const TITULO_1 = '¡Felices Fiestas';
const TITULO_2 = 'Patrias!';
/**
 * La bajada, en versales y partida a mano en tres líneas como la referencia.
 * Dejada al navegador cae «…familia / DoubleTree.» con una palabra sola, que es
 * un defecto documentado.
 */
const SUBTEXTO = ['GRACIAS POR SER PARTE', 'DE NUESTRA FAMILIA', 'DOUBLETREE.'];

const CUERPO = {titulo: 128, subtexto: 50} as const;

/**
 * ⭐ RONDA 4 — EL LOGOTIPO CRECE. Eli: «agranda más el logo, para compensar un
 * poco la jerarquía». La plantilla `logo-ST.png` lo deja en 167 @1080 = 15,5 %
 * del ancho, y el titular de esta pieza ocupa el 75 %: el logotipo no sostenía
 * el otro extremo. A 225 queda en 20,8 %.
 *
 * ⚠️ Es una excepción de TAMAÑO y nada más: sigue centrado, sigue en su tope de
 * 241 y sigue a su proporción real de 1,2254. **El logotipo no se deforma.**
 * Y no se hereda: la próxima historia de DT vuelve a la plantilla salvo que Eli
 * diga lo contrario.
 */
const LOGO_ANCHO = 225;

/**
 * ⭐⭐ RONDA 5 — LAS BANDERAS SON DOS, Y FLANQUEAN EL TEXTO.
 *
 * La ronda 4 puso UNA bandera asomando por la esquina superior izquierda. Eli
 * devolvió la pieza con **dos marcas rojas, una a cada lado del bloque de texto**,
 * a la altura de «Patrias!» y la bajada, y las dos apuntando hacia afuera.
 *
 * ⭐ Y eso ya estaba resuelto en el estudio: es el mismo pedido que hizo en la
 * ronda 5 de la S3 de Between —«pon dos banderas en la dirección que te dejo»— y
 * de ahí sale el truco del `espejo`: **va POR FUERA de la rotación**, así que un
 * par con el MISMO `giro` y `espejo` en una de las dos queda simétrico, con las
 * dos apuntando hacia afuera. Con el espejo por dentro habría que invertir el
 * giro a mano y cualquier ajuste posterior descuadra el par.
 *
 * ⭐ LA ALTURA SE MIDIÓ, como la esquina de la ronda 4:
 *
 *     y 660-880   izq **10,53:1**  sd 24,0     der **8,29:1**  sd 35,4   ← la elegida
 *     y 690-956       8,72:1  sd 30,9              7,36:1  sd 38,7
 *     y 620-840       8,64:1  sd 37,5              8,27:1  sd 41,1
 *
 * ⚠️ Y EL ANCHO LO FIJA EL TEXTO, no el gusto. Medido sobre el PNG, la bajada más
 * larga va de x=236 a x=843: las banderas tienen que quedar fuera de esa columna o
 * se meten debajo de las versales. Con ancho 205 en x=14 y x=861 quedan a ~32 px
 * de cada canto del texto.
 */
const BANDERAS = {
  /**
   * ⭐ RONDA 6 — Eli: «quiero que quede al límite el palito de la bandera, aumenta
   * un poco la bandera de tamaño, muy sutil».
   *
   * 205 → **235**. Y las dos se corren hacia afuera hasta que **la punta del
   * mástil cae EXACTO en el canto** de la pieza.
   *
   * ⭐ El desplazamiento NO se busca a ojo: se calcula. El mástil termina en
   * (56, 224) del viewBox y todo el dibujo gira 18°, así que su punta real cae en
   * **(31,6 · 190,4)** ya rotada. A escala 235/300 = 0,7833 eso son 24,7 px, y de
   * ahí salen los dos valores:
   *
   *     izquierda  x = −24,7  ⇒ la punta aterriza en x = 0
   *     derecha    x = 869,7  ⇒ la punta aterriza en x = 1080
   *
   * ⚠️ Y el ancho sigue teniendo techo, que lo pone el texto: a 235 la tela llega
   * hasta x = 200 y la bajada más larga empieza en x = 236 — 36 px de aire. A 245
   * quedarían 27 y a 265 ya se tocan.
   */
  ancho: 235,
  y: 660,
  giro: 18,
  opacidad: 0.78,
  xIzquierda: -24.7,
  xDerecha: 869.7,
} as const;

const Y = {
  tituloCaja: 540,
  /**
   * ⭐ RONDA 3: sube 35 px. Con el interlineado apretado, el salto ENTRE niveles
   * quedaba en 76 px contra 24 dentro del titular — más de 3×, y la regla del
   * estudio es ~1,5×. Acá da 815 → 46 px contra 24, o sea 1,9×.
   */
  bajada: 815,
} as const;

/**
 * ⭐ RONDA 3 — INTERLINEADOS. Eli: «ajusta un poco el espaciado de abajo de los
 * párrafos, están muy separados entre párrafos», con dos marcas rojas: una entre
 * las dos líneas del titular y otra entre las líneas de la bajada.
 *
 * Medido sobre el PNG de la ronda 2 y corregido al ojo de la regla, no al azar:
 *
 *   titular · hueco de tinta 47 px al cuerpo 128 = 0,37 em  →  queda en 0,19 em
 *   bajada  · hueco de tinta 31 px al cuerpo  50 = 0,62 em  →  queda en 0,30 em
 *
 * ⚠️ En una pila de `div`, lo que abre el hueco es el `line-height` de la línea
 * de ARRIBA, no el de la de abajo: por eso el titular 1 baja a 0,90 y el 2 se
 * queda en 1,0 (es el último y su caja ya no empuja a nadie).
 */
const INTERLINEA = {titulo1: 0.9, titulo2: 1.0, bajada: 1.08} as const;

/**
 * LA BANDERA DE CHILE EN UNA TINTA.
 *
 * ⭐ La geometría está COPIADA, no importada, de `BetweenIlustraS3.tsx`. Es a
 * propósito y respeta las dos reglas a la vez:
 *
 *   · **No se importa** porque el criterio de una marca no se traspasa a otra, y
 *     un archivo de Between no tiene nada que hacer dentro de una pieza de DT.
 *   · **Pero la geometría sí se reusa**, porque no es criterio de Between: es la
 *     bandera de Chile, y el estudio ya le corrigió dos defectos que sólo se ven
 *     al zoom (ver abajo). Redibujarla de cero sería volver a cometerlos.
 *
 * ⚠️ Lo que SÍ cambia respecto de Between: **el trazo va limpio**. En Between la
 * ilustración lleva textura de mano; DT es «elegante, minimalista y sencillo», así
 * que acá es línea pura.
 *
 * Cómo se lee chilena sin el tricolor: cantón cuadrado arriba a la izquierda,
 * estrella de cinco puntas calada, y división horizontal. Ninguna otra bandera
 * tiene esa combinación. Y ondea, con el mástil corto y todo inclinado — una
 * bandera recta y rectangular se lee como un ícono de menú de idioma.
 */
const VB_BANDERA = {w: 300, h: 270};

const BanderaChile: React.FC<{
  ancho: number;
  tinta: string;
  /** Con qué color se cala la estrella sobre el cantón macizo. */
  fondo: string;
  trazo?: number;
  giro?: number;
  /**
   * Espeja la banderita, para poner un par flanqueando el texto.
   *
   * ⚠️ El espejo va POR FUERA de la rotación. Con el mismo `giro`, la espejada
   * apunta al lado contrario: un par con `giro` igual y `espejo` en una de las dos
   * queda simétrico, las dos hacia afuera. Es lo que pidió Eli marcando dos
   * banderas a los lados del bloque de texto.
   */
  espejo?: boolean;
}> = ({ancho, tinta, fondo, trazo = 5, giro = -8, espejo = false}) => {
  const escala = ancho / VB_BANDERA.w;
  return (
    <svg
      width={ancho}
      height={VB_BANDERA.h * escala}
      viewBox={`0 0 ${VB_BANDERA.w} ${VB_BANDERA.h}`}
      fill="none"
      stroke={tinta}
      strokeWidth={trazo}
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden
    >
      <g transform={espejo ? `translate(${VB_BANDERA.w} 0) scale(-1 1)` : undefined}>
      <g transform={`rotate(${giro} 150 130)`}>
        {/* el mástil */}
        <path d="M56,224 L56,36" strokeWidth={trazo * 1.4} />
        {/* la tela: los bordes de arriba y abajo ondean, y el borde libre es una
            S — es lo que le da el vuelo */}
        <path d="M56,44 C112,30 168,58 224,44 C244,39 258,42 268,48 C258,74 258,112 268,140 C258,146 244,149 224,144 C168,158 112,130 56,144 Z" />
        {/* La división horizontal, sobre la misma onda de la tela.
            ⚠️ TERMINA EN x=256 y no en 266: el borde libre pasa por x≈268 y, con la
            punta redondeada del trazo, la línea SOBRESALÍA — al zoom se veía una
            espina saliendo por la derecha. */}
        <path d="M56,94 C112,80 168,108 224,94 C240,90 250,92 256,95" />
        {/* El cantón: cuadrado, arriba a la izquierda, macizo.
            ⚠️ Sus bordes de arriba y de abajo van SOBRE las mismas curvas de la tela
            y de la división, evaluadas en x=118: arriba y≈41, abajo y≈91. Puestos a
            ojo (48 y 99) el cantón quedaba 14 px más abajo que la división y al zoom
            se veía un escalón en su esquina inferior derecha. Si se mueve la onda de
            la tela, hay que volver a evaluar estos dos puntos. */}
        <path d="M56,44 C77,38 98,39 118,41 C117,58 117,76 118,91 C98,88 77,89 56,94 Z"
              fill={tinta} />
        {/* La estrella de cinco puntas, calada. Centro (87,66) —el centro real del
            cantón— radio 21 y radio interior 8,6. */}
        <path
          d="M87,45 L92.1,59 L107,59.5 L95.2,68.7 L99.3,83 L87,74.6 L74.7,83 L78.8,68.7 L67,59.5 L81.9,59 Z"
          fill={fondo}
          stroke={fondo}
          strokeWidth={trazo * 0.5}
        />
      </g>
      </g>
    </svg>
  );
};

/** Dibuja un texto con `¡`/`¿` en Stag volteando el signo de apertura (truco de Eli). */
const TextoStag: React.FC<{texto: string; style: React.CSSProperties}> = ({texto, style}) => (
  <div style={{...style, whiteSpace: 'nowrap'}}>
    {volteaApertura(texto).map((t, i) =>
      t.flip ? (
        <span key={i} style={{display: 'inline-block', transform: 'rotate(180deg)'}}>
          {t.t}
        </span>
      ) : (
        <span key={i}>{t.t}</span>
      ),
    )}
  </div>
);

export const DtStFiestasPatrias: React.FC<{guia?: boolean}> = ({guia = false}) => (
  <AbsoluteFill style={{backgroundColor: DT.colores.azul}}>
    {/* 1 · El mosaico, ya con su filete y su velo (ver el script del collage) */}
    <Img
      src={staticFile('assets/hilton/dt/st-fiestas-collage.jpg')}
      style={{width: '100%', height: '100%', objectFit: 'cover'}}
    />

    {/*
      2 · Las dos banderas, flanqueando el bloque de texto.

      Van con el MISMO `giro` y `espejo` en la de la derecha: así el par queda
      simétrico y las dos apuntan hacia afuera, que es como Eli las marcó.

      La estrella se cala en AZUL DT, que es el color del velo que tienen detrás.
    */}
    {[
      {x: BANDERAS.xIzquierda, espejo: false},
      {x: BANDERAS.xDerecha, espejo: true},
    ].map((b) => (
      <div
        key={b.x}
        style={{
          position: 'absolute',
          left: b.x,
          top: BANDERAS.y,
          opacity: BANDERAS.opacidad,
        }}
      >
        <BanderaChile
          ancho={BANDERAS.ancho}
          tinta={DT.colores.blanco}
          fondo={DT.colores.azul}
          trazo={4.2}
          giro={BANDERAS.giro}
          espejo={b.espejo}
        />
      </div>
    ))}

    {/* 3 · El saludo, en itálica */}
    <div
      style={{
        position: 'absolute',
        left: 0,
        top: Y.tituloCaja,
        width: 1080,
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
      }}
    >
      <TextoStag
        texto={TITULO_1}
        style={{
          fontFamily: DT.fuentes.titular,
          fontStyle: 'italic',
          fontWeight: DT.pesos.medium,
          fontSize: CUERPO.titulo,
          lineHeight: INTERLINEA.titulo1,
          color: DT.colores.blanco,
        }}
      />
      <TextoStag
        texto={TITULO_2}
        style={{
          fontFamily: DT.fuentes.titular,
          fontStyle: 'italic',
          fontWeight: DT.pesos.light,
          fontSize: CUERPO.titulo,
          lineHeight: INTERLINEA.titulo2,
          color: DT.colores.blanco,
        }}
      />
    </div>

    {/*
      4 · La bajada, en VERSALES itálicas.

      ⭐ El tracking no es adorno: en versales las letras vienen pensadas para ir
      dentro de una palabra en caja baja, y sin abrirlas se apelmazan. 0,04em es
      lo que necesita esta línea para respirar sin desarmar la palabra.
    */}
    <div
      style={{
        position: 'absolute',
        left: 0,
        top: Y.bajada,
        width: 1080,
        fontFamily: DT.fuentes.titular,
        fontStyle: 'italic',
        fontWeight: DT.pesos.regular,
        fontSize: CUERPO.subtexto,
        lineHeight: INTERLINEA.bajada,
        letterSpacing: '0.04em',
        color: DT.colores.blanco,
        textAlign: 'center',
      }}
    >
      {SUBTEXTO.map((linea) => (
        <div key={linea}>{linea}</div>
      ))}
    </div>

    {/*
      5 · Logotipo principal, BLANCO y centrado, ARRIBA.

      ⭐ RONDA 3 — Eli: «deja el logo principal arriba donde debe estar». Vuelve
      al tope de la plantilla `logo-ST.png` (241) y centrado. La referencia lo
      bajaba al tercio inferior y eso queda sin efecto: la plantilla es de la
      marca y manda sobre la referencia.

      ⭐ RONDA 4 — y crece de 167 a 225 para sostener la jerarquía (ver `LOGO_ANCHO`).

      Se escala UNIFORME por su proporción real — jamás por geometría.
    */}
    <Img
      src={staticFile('assets/hilton/dt/logo-dt-blanco.png')}
      style={{
        position: 'absolute',
        top: G.logoYStory,
        left: (1080 - LOGO_ANCHO) / 2,
        width: LOGO_ANCHO,
        height: LOGO_ANCHO / G.logoProporcion,
      }}
    />

    {/* 6 · Guía de zonas seguras — sólo para revisar, NO se entrega */}
    {guia ? (
      <>
        <div
          style={{
            position: 'absolute',
            top: 0,
            left: 0,
            width: 1080,
            height: DT.seguras.story.arriba,
            background: 'rgba(255,0,110,0.22)',
            borderBottom: '2px dashed rgba(255,0,110,0.9)',
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: 0,
            left: 0,
            width: 1080,
            height: DT.seguras.story.abajo,
            background: 'rgba(255,0,110,0.22)',
            borderTop: '2px dashed rgba(255,0,110,0.9)',
          }}
        />
      </>
    ) : null}
  </AbsoluteFill>
);

export const DtStFiestasPatriasGuia: React.FC = () => <DtStFiestasPatrias guia />;
